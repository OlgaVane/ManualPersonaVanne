using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using UTTT.Ejemplo.Linq.Data.Entity;

namespace UTTT.Ejemplo.Persona.Modelo
{
    public class ClsSesiones
    {
        //public static Usuario BuscarPorId(int idUsuario)
        //{
        //    DcGeneralDataContext dcGlobal = new DcGeneralDataContext();
        //    Usuario usuario = null;
        //    var query = dcGlobal.Usuario.Where(p => p.id == idUsuario).Select(p => p);
        //    if (query.Count() > 0)
        //        usuario = query.First();
        //    return usuario;
        //}
    }
}